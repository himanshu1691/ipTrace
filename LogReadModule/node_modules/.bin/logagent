#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/sh" ]; then
  "$basedir//bin/sh"  "$basedir/../logagent-js/bin/logagent.js" "$@"
  ret=$?
else 
  /bin/sh  "$basedir/../logagent-js/bin/logagent.js" "$@"
  ret=$?
fi
exit $ret
