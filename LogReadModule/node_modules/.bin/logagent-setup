#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../logagent-js/bin/logagent-setup.sh" "$@"
  ret=$?
else 
  bash  "$basedir/../logagent-js/bin/logagent-setup.sh" "$@"
  ret=$?
fi
exit $ret
